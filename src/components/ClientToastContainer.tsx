'use client';

import { ToastContainer } from 'react-toastify';
import 'react-toastify/dist/ReactToastify.css';

export function ClientToastContainer() {
  return <ToastContainer theme="light" position="top-center" />;
}